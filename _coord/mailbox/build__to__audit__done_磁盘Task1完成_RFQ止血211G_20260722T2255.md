# build → audit / strategy：DONE — 磁盘 Task 1 完成（RFQ 止血 211.9 GiB）

发件：build（工程线）· 2026-07-22T22:55
收件：audit（抄送 strategy）
收据（box）：`/home/ubuntu/rfq-durable-copy-2026-07-{17,18,19}-*.json`

---

## 一句话

✅ **操作员「第一件·磁盘定向验证」完成。** 三日 RFQ 存量全部循证清除，**已验证==已删除、零跳过、采集零中断**。磁盘 129G 空闲(83%) → **336G 空闲(54%)**。

## 分日结果（双门：put.SHA256==head.SHA256==base64(本地sha256) 且大小一致）

| 日期 | 验证 | 删除 | 释放 | 跳过 | S3 验证副本 |
|---|---|---|---|---|---|
| 07-17 | 186 | 186 | 37.2 GiB | 0 | 186 |
| 07-18 | 346 | 346 | 78.2 GiB | 0 | 346 |
| 07-19 | 422 | 422 | 96.5 GiB | 0 | 422 |
| **合计** | **954** | **954** | **211.9 GiB** | **0** | **954** |

- 每日「已验证数==已删除数」；三日本地 rfq 现全 = 0。
- 采集全程无扰：firehose/l2/rfq 收官时均在当前小时(22:52)正常写入。
- 副本落 `s3://kalshi-vault-ritcardo/ec2/raw-rfq-durable/date=<D>/`，每对象 `verification_state==EXACT_VERSION_FULL_SHA256`、VersionId 锁定、S3 独立算 SHA 与本地一致。

## 硬约束全部遵守

- 不碰采集进程 ✓；07-20 及未成熟日不碰 ✓；验证不过绝不删（本次 0 跳过，全过）✓；删除报告已验证==已删除 ✓。

## 请 audit 出 ACCEPTANCE

建议按 `build__to__audit__B14查无实据…` 里两条口径核：①收据每对象三 SHA 相等 + 大小一致；②已验证==已删除 + 采集连续。收据在 box 上述路径，可独立复核。

## 关联事项（已另发，勿混）

1. **B14**：查无实据，请复核后改判/关单，不启动 F-2 冒险部署（`build__to__audit__B14查无实据…`）。
2. **RFQ 方案 A**：操作员已定 = 纳入统一封存链；本次止血脚本**不固化**，方案 A 上线即弃用；W-RFQ-UNIFY-01 已交 strategy 排队列（`build__to__strategy__操作员定RFQ方案A…`）。
3. **落盘 zstd**（~120G/天→~10G/天）是磁盘根治，建议排承重墙维护，优先于（不存在的）B14。

—— build（Task 1 完成）
