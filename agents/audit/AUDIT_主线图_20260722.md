# 审计结论：PLAN_SPORTS_TRADING_MASTER（主线图）

审计：Audit（Cowork Claude）· 2026-07-22
受审对象：`docs/PLAN_SPORTS_TRADING_MASTER.md`（11 站版）+ `strategy__to__audit__...20260722T1630.md`
依据：`agents/audit/ACCEPTANCE_主线图_ROADMAP_TO_LIVE.md` 七条

## 裁决：**条件通过（CONDITIONAL PASS）**
5 条干净通过 · 2 条需补（C-1/C-2）· 规则6 正确挂起 · 另有 2 项发现（F-1/F-2），**F-2 必须在 B14 动工前处理**。
补完 C-1/C-2 即为完全通过，不需重审全文。

---

## 逐条打勾

| # | 规则 | 结论 | 依据 |
|---|---|---|---|
| 0 | 不碰正在跑的东西 | ✅ **通过** | §0/§6 声明齐全；更关键的是**内容层**核过：站1 标 ✅ 且称"全项目唯一稳固的地基"；crosswalk 把 DATA_COMPLETENESS 标为"承重墙 ✅ 维持"；第 228 行明文"capture/seal/S3 upload 一律不得标为噪音/降级/丢弃"。**全文无任何一站会导致采集/封存被降级、暂停或延后。** |
| 1 | 收编≠删除 | ✅ **通过（附注）** | §5 crosswalk 覆盖 5 份旧图 + deep03 各 Layer；第 228 行显式声明无门被丢弃并承诺后续补映射。**附注见 C-2。** |
| 2 | 日期=B14 的函数 | ✅ **通过，且超出要求** | 全文无"月底"；写死「验证就绪日 = B14 落地日 + 20 个干净封存日」+ 后果量化。**更进一步：主动声明现有 3 个干净 L2 日不计入（B14 前采集、坏钟风险未除），严格口径下时钟尚未起步。** 这是比我要求更狠的诚实，记一功。 |
| 3 | 串行/并行标出来 | ⚠️ **需补（C-1）** | 回信声称已标，但文档本体只有零散隐含（如站4"合成/离线部分可先做"）。**缺一个显眼的并行轨标注。** |
| 4 | 可证伪过关条件 | ⚠️ **基本通过，一处需补（C-2）** | 站3/4/5.5/6/8/10 的退出条件均含数字或可对账物，合格。**站2 的"L2 干净率回到接近全覆盖"没有数字**，不可证伪。 |
| 5 | C1 缺口独立成关 | ✅ **通过，做得好** | 新增站 5.5，受影子引擎 10 条门管辖，过关条件可证伪（悲观口径费后 PnL>0 且 3 日符号一致 + 显示/成交差距量化入 spec），并明写队列位置/穿价/撤单延迟三样在 C1 数据里"完全没有"。**正是防止 C1 拿假数字晋级的那道门。** |
| 6 | 旧图加 superseded + 单写者 | ⏸ **正确挂起** | 策略线主动推迟到审计 PASS 后再加、只加一行不删旧门、在正确分支操作。**顺序判断正确，计划批准，本次 PASS 后执行。** |
| 7 | 不自审（build≠judge） | ✅ **通过** | 策略线**主动作废了自己已拿到 PASS 的自审**并交出主线图。这是机制真正起作用的证据，记一功。 |

---

## 必补项（补完即完全通过）

**C-1 · 并行轨必须显式标注（规则3）**
站 3 是日历约束、催不动，**唯一不浪费它的办法就是等待期并行推进站 4 / 站 5.5**。文档必须在第 2 节加一个显眼标注（每站加 `[串行·必须等]` / `[并行·等待期可做]` 字段，或加一张两轨图），让人一眼看到"等 20 天的同时有活在干"。
**不补的后果**：读图的人自然按站号 1→10 顺序理解，把可并行的引擎线排到 20 天之后 = 白等一个月。这正是这张图要治的病。

**C-2 · 站 2 退出条件补数字（规则4）**
"L2 干净率接近全覆盖"不可证伪。改成可核的门，例如：**「连续 N 个封存日的 L2 帧 `recv_mono_ns` 全部落在合法区间、坏帧率 < X%、且无 FULL/TRADE_INSERT 崩溃」**，N/X 由工程线给候选值、操作员拍板。

**（规则1 附注）** crosswalk 是**组/阶段级**映射（如"F 组 → 站5-6"），不是**逐门级**。组级映射不能保证组内某一道具体门没被漏掉。不阻塞本次通过，但要求：**每一站激活时，对该站映射到的组做一次逐门核对**，写进该站的开工检查。

---

## 新发现

**F-1 · 作废的自审文件必须打上 VOID（轻，但会咬人）**
`docs/plan_audits/AUDIT_PLAN_SPORTS_TRADING_MASTER_2026-07-22.md` 是那份已作废的自审，内容是 PASS。**它现在躺在 plan_audits 目录里，三周后任何人（或任何 agent）读到它，会当成一次真实的审计通过。**
要求：文件顶部加 `**VOID — 自审，按 build≠judge 作废，不构成审计结论。有效审计见 agents/audit/AUDIT_主线图_20260722.md**`。

**F-2 · B14 的修复本身会碰采集热路径——这是规则0 的真实风险所在（要紧）**
瓶颈 a 的解药是**采集端 ws_shadow 的 C++ 修改 + 部署**。部署意味着重启采集进程 → **可能造成 capture 断档**，而 capture 断档正是规则0 要保护的、时间不可逆的东西。
**"赶紧修 B14"绝不能变成"随手重启采集"。** B14 的 W 必须包含：

1. **零断档或最小断档的部署方案**（热替换 / 双写切换 / 明确的最短停机窗口），并**事先量化断多久**；
2. **操作员授权的部署窗口**（低活跃时段，由操作员拍板，不由 agent 自选）；
3. **部署前后 capture 连续性的证明**（帧计数/时间戳连续性核对，不是"看起来在跑"）；
4. **首个干净日的持久化标记（关键）**——全项目的日程现在是 B14 落地日的函数，**必须有一条不可争议的记录写明"哪一天起的封存日算干净"**（落进 seals 元数据或 DECISIONS.md）。**没有这个标记，三周后没人能就"20 天从哪天开始"达成一致**，规则2 的诚实就白做了。

**B14 分工建议（回答策略线的请示）**：策略线判断正确，不该主会话顺手改。建议 **build/工程线领 W 实现 + 部署方案；Audit 出验收口径（即上面 4 条 + "新数据 recv_mono_ns 落在合法区间"的可核检验）；操作员授权部署窗口。** 策略线不必先写 W 规格，直接由工程线按 PIPE_DEBT_PAYDOWN_PLAN 的 W-B 走。

---

## 给操作员的一句话
**这张图通过了,而且它最有价值的一行是主动承认"20 天的钟还没起步"——那是把一个好看的假日期换成了一个难看的真日期。** 补完 C-1（并行轨）和 C-2（站2 数字门）即完全通过。**动 B14 之前先看 F-2:修它的过程本身有断采集的风险,别用一次断档去换一个早一天的时钟。**
