[Unit]
# Installed only when the ephemeral credential broker and its independently
# reviewed identity evidence are both present.  The durable and full publisher
# retain independent retry timers; this edge removes avoidable success latency.
OnSuccess=kalshi-research-v3-daily.service
