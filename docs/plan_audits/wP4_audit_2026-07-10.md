# W-P4 independent audit (2026-07-10) — VERBATIM

Auditor: fresh-context agent, no part in implementation; pure-math + hand
derivation (no network). Scope: commit 5b1f4d5 (tests/test_pricing_pipeline.py
+ tests/fixtures/pricing_scenarios/*.json) vs PLAN_PRICING_MODEL §3 W-P4 +
GUARDRAILS D2/E1.

Disposition (executor, same session): verdict ACCEPT-WITH-FINDINGS — the
strongest result of the P-track (the auditor independently HAND-DERIVED every
fixture value and all matched; red-proof bites; frozen cores untouched). Both
non-blocking findings applied. N1: added a per-scenario-class red-proof (each
class made RED by a relevant transient mutation) so "one seeded defect per
class" is satisfied literally, not only for the imbalanced scenario. N2:
value-pinned the wind-down t=400 δ/cap + strict monotonicity in the two
trend tests. 10→11 tests. Fix commit: see SESSION_LOG.

---

## VERDICT: ACCEPT-WITH-FINDINGS

The core of W-P4 — fixture correctness — is clean. I independently hand-derived every expected value in all 6 scenarios (~15 quote points plus lo intermediates) from the raw math without trusting the pipeline, and every one matches the committed JSON exactly. The red-proof genuinely bites. Frozen-writes, guards, and registration are all intact. Findings are minor completeness/coverage observations, none blocking.

## A. Fixture correctness (the core) — PASS
Re-derived by hand (throwaway probes reimplementing logit/expit/half-width/emit independently of tools/pricing/*):
- calm inv 0/+5/−5 → 4400/5600, 4200/5600, 4400/5800 ✓ (gamma_t=0.01, δ=0.225/0.255)
- imbalanced-wide-book (10c/30c, 100/300): lo-space micro −1.8597 → 1100/1700 ✓; price-space micro (px 1500e4, lo −1.7346) → 1200/1900 — Jensen gap real and material
- bracket legs: excess 0.08, corr [0.006667,0.006667,0.066667], corr_lo −0.433322/−0.433322/−1.304949 → 3400/4500, 1700/2600 ✓; thin leg moves 10× ✓
- buy-pressure drift 0/0.05/0.10 → 4400/5600, 4500/5700, 4600/5900 ✓ (monotone)
- winddown δ 0.225/0.2625/0.2833, cap 12.5/6.25/2.78 → …, Q6 hard stop at t=200 ✓
- jump velocity 0.001374 ≥ 0.0010 → trips ✓; trade-only (1e9, still book) → no trip ✓
No fixture value is wrong. Genuine independent spec, not a tautology.

## B. Red-proof / per-class falsifiability — N1 (minor, non-blocking)
The single committed price-space mutation bites only the imbalanced scenario (the others use balanced books where lo==price). The plan says "one seeded-defect run per scenario class." D2 intent IS met — I proved each class fails under a relevant impl mutation (flip reservation sign → calm RED; zero drift → buy_pressure RED; kill settle_widen → winddown RED). Gap is between the literal "committed run per class" and "one representative committed run + falsifiable exact goldens." Worth a note, not a reject.

## C. Exactness / tolerances — PASS
Committed lo intermediates vs exact: leg0 corr_lo off 6.3e-8, leg2 off 2.8e-7, δ/cap exact — inside abs=1e-6, not masking a wrong value. Cent-exactness + lo-pin on δ/cap/corr adequate for the C++ port.

## D. Scenario coverage — PASS (N2 minor)
Each scenario exercises its named property. N2: winddown step 2 (t=400) δ checked only by the non-strict monotonicity test; and the buy-pressure/winddown monotonicity uses non-strict sorted(). Fully backstopped by the exact-cent goldens, no real hole.

## E. Frozen-writes / scope — PASS
git show 5b1f4d5 touches only the 6 fixtures + test + tools.json + run_pipeline + RESULT. tools/pricing/{lo,fair,quote}.py diffs EMPTY (Forbidden-writes intact). Red-proof uses monkeypatch (transient). tracked-fixtures guard passes. test_pricing_pipeline registered pure. check_registry ok.

## F. JSON/driver robustness — PASS
Every expectation key across all fixtures is asserted by the driver — no orphan key → no false-green surface. JSON null → None compares correctly for suppressed sides.

## G. What I ran
./tests/run_pytest.sh tests/test_pricing_pipeline.py → 10 passed; make check → ALL PASS; tracked-fixtures guard + check_registry ok; independent hand-derivation probes for all 6 scenarios (matched); mutation probes (price-space per book scenario; gamma-sign / drift-zero / settle-widen — each turns its scenario RED).
