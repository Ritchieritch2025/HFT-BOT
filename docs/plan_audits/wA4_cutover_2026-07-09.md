# W-A4 — zero-gap cutover — validation log (2026-07-09)

**Verdict: ✅ CUTOVER COMPLETE.** Capture moved Mac→EC2 across a 4 h 38 m
transition (18:30:57→23:09:07 UTC) during which at least one machine was
always capturing — the three single-machine outages (Mac 20 s at step 3;
EC2 ~30 s at each of the 19:20:35 and 19:25:52 restarts, per journalctl —
audit finding 1) were each fully covered by the other machine (Mac hour-19+
reports COMPLETE/dropped=0). The lossless-channel diff proves zero
cross-machine loss. EC2 is now sole
owner of WS + REST; the Mac is dormant (launchd installed-not-loaded, data
untouched, sleep still disabled until W-A5). Operator was present and gave
three explicit go/no-go approvals (steps 1, 3, 5).

## Timeline (all UTC 2026-07-09, from command outputs in-session)

| Time | Step | Evidence |
|---|---|---|
| pre | Prereqs: EIP 3.130.232.109 ✓ · work/live ✓ · **delete-denial LIVE-TESTED by operator: `aws s3 rm` ⇒ AccessDenied, RM_RC≠0** · egress-443 DEFERRED by operator ruling to a before-live (S1) hard gate (recorded in SECURITY_CHECKLIST_EC2.md §4) · box↔Kalshi 443=200, S3 list OK | session outputs; checklist §4 |
| 18:30:57 | **Step 1** (GO #1): EC2 pipeline started with `work/live/rest_disabled` set; catalog pre-seeded from the S3 vault (restore path, zero Kalshi REST) | supervisor.out.log: "REST disabled … skipped"; ws_shadow start line: env=prod read_only orders_blocked |
| 18:31–19:02 | **Step 2 soak (29.5 min, crossed the 19:00 hour boundary)**: 1,541,582 events (203,699 trade / 1,337,883 tick) ≈ 881/s sustained; reconnects=0; errors/overflow=0/0; recorder dropped=0 → COMPLETE; missed-pong=0; transmitted=0; write-tokens=0; WS SHADOW PASS; rotation fired correctly (`firehose_19.ndjson`, seconds=3590) | ws_shadow hour-18 exit report (box log) |
| 19:17:36–56 | **Step 3** (GO #2): Mac REST off — flag + launchd reload; Mac WS gap **20 s**, covered by verified-healthy EC2 | Mac supervisor.out.log: "REST disabled … skipped" |
| 19:18:53 | **Step 4a**: EC2 flag removed | FLAG-REMOVED output |
| 19:19–19:25 | **INCIDENT 1 (found live): catalog_sync HTTP 429.** Root cause: EC2's <1 ms RTT fires the sequential pagination loop ~30× faster than the Mac's ~30 ms RTT — the latency win itself burned the 600-token read burst. The Mac never saw this because network latency throttled it for free. FIX: `paced_open` (50 ms pacing ≤20 req/s + exponential 1,2,4,8,16 s backoff on 429), 5 red-first unit tests with fake opener/clock, wired into run_pipeline | commit 8e6d703; catalog.log tracebacks (both 429s pre-fix) |
| 19:25:52–19:29 | **Step 4 verified**: full catalog crawl ZERO 429 — series 11,295 / events / open markets ~50k (250+ pages) / settlements, all four parquets fresh | catalog.log + parquet mtimes |
| 19:29–19:38 | **INCIDENT 2 (found live): dim_snapshot Binder Error** (timestamp corrected by audit: the error followed the 19:25:52 run; fix commit 3f3d474 at 19:38Z; the manual chain verification ran later ~23:05) — the fresh crawl's markets parquet has floor_strike but NO cap_strike column (no open market carried it; union_by_name creates only columns present in data); the strike SQL referenced it unconditionally. FIX: expression built from columns actually present (D3), offline regression tests both modes | commit (dim_snapshot fix); box run: DIM SNAPSHOT PASS 3 tables; build_classification: 11,295 series → A=6,194 / B=5,101 |
| 23:09:07–17 | **Step 5** (GO #3): Mac `launchctl unload` — all Mac pipeline processes stopped in 10 s; plist retained | MAC_PIPELINE_ALL_STOPPED + plist ls |
| 23:09:49 | Post-cutover: EC2 capture growing (+8.5 MB/20 s), 365,054 events in hour 23, zero errors | box status output |

## Dual-machine capture-completeness report (acceptance)

Window: **18:32:00–18:59:00 UTC** (both machines fully live, edges trimmed).
Tool: `sandbox/wa4_capture_diff.py` (P8) + trade_id/exchange-ts refinement.

**Method note (deviation from the plan's "aligned by ws_seq" phrasing):**
Kalshi's ws `seq` is per-subscription-per-connection — NOT comparable across
machines. seq/reconnect/drop counters prove *within*-machine continuity
(reconnects=0, recorder dropped=0, errors=0 on BOTH boxes for hour 18);
*cross*-machine completeness is proven by content alignment:

| Metric | Result |
|---|---|
| **Trades (lossless channel, globally unique trade_id, windowed by exchange ts_ms)** | mac=190,815 unique · ec2=190,815 unique · **missing_on_ec2=0 · missing_on_mac=0** |
| **Single-machine miss rate (lossless channel) = the seq_gaps counter baseline (W-C5)** | **0.000000% both directions** |
| Ticker channel raw-frame diff | symmetric 197,231 / 196,977 "differences" at near-identical message volume (1,242,791 vs 1,242,534, Δ0.02% — committed-tool numbers, audit finding 5) — **NOT loss**: ticker is a per-connection CONFLATED state feed. Proof: (a) equal volume with ~11% differing states even after stripping ts/ts_ms/time (state-keyed pass: overlap 1,100,981); (b) trades interleaved on the SAME TCP stream show zero loss — random ~11% client loss would hit both channels; (c) the raw-window 3 trade "misses" vanish under exchange-ts alignment (arrival-skew edge artifact, audit-confirmed) |
| Within-machine seq continuity (audit's independent bonus check) | **per-connection trade-subscription `seq` continuous with ZERO gaps across the entire hour-18 file on BOTH machines** (Mac 3,246,112 lines / EC2 1,541,584 lines, 0 unparseable) |
| Mac hour-18 (full hour, own counters) | 3,246,110 events, reconnects=0, dropped=0, COMPLETE |

**Finding worth keeping (new fact about the feed):** the ticker channel is
conflated per connection — two subscribers do NOT receive identical update
streams. Consequences: (1) cross-machine ticker diffs are semantically
bounded, use trades for loss measurement; (2) any future strategy math must
not assume the ticker stream is a complete event log (the orderbook_delta
channel is the lossless book source — Q5). Candidate for kalshi_facts.yaml.

## no-fabrication 溯源表

| Number | Source |
|---|---|
| All soak/report counters (1,541,582 events; 0 reconnects; 0 drops; etc.) | ws_shadow hour-18 exit report, `~/hft-bot/work/live/ws_shadow.log` (box), quoted verbatim in-session |
| Diff numbers (190,815/0/0; 1,243,417 vs 1,242,494; 1,100,981 overlap) | `sandbox/wa4_capture_diff.py` + inline trade_id pass over `work/raw/date=2026-07-09/firehose_18.ndjson*` (Mac) vs `/tmp/wa4_ec2/firehose_18.ndjson*` (EC2 copies); outputs quoted in-session |
| Step timestamps | `date -u` in the same command as each action (quoted above) |
| Catalog/classification numbers (11,295 series; A=6,194/B=5,101; 80,000 events/markets) | catalog.log + dim_snapshot/build_classification stdout on the box |
| Mac 20 s step-3 gap | STEP3_START/LOADED timestamps 19:17:36→19:17:56 |
| 429 root cause | both tracebacks pre-fix in catalog.log (lines 46/92); post-fix crawl zero 429 (`grep -c 429` count unchanged at 2 after the clean run) |

## Deviations (declared)

1. Egress-443 prerequisite DEFERRED by operator ruling (2026-07-09) to a
   before-live S1 hard gate — recorded in SECURITY_CHECKLIST_EC2.md §4.
2. "aligned by ws_seq" implemented as within-machine seq continuity +
   cross-machine content alignment (see method note — per-connection seq
   makes literal cross-machine seq alignment meaningless).
3. Two production fixes shipped MID-CUTOVER (both found live, both
   red-first-tested, both required for EC2 to function as REST owner):
   catalog_sync pacing (8e6d703) and dim_snapshot schema-drift. `make
   check` + `run_pipeline` green on both platforms after each.
4. Step 3 implemented as flag + launchd reload (the running Mac supervisor
   predated the flag-gate code); its 20 s WS gap was EC2-covered by design.

## Rollback posture (post-step-5, honest)

Mac launchd is installed-but-dormant; Mac data untouched; `pmset
disablesleep` still ON. A rollback now = `launchctl load` + resubscribe,
and must backfill the unload→reload hole from EC2/S3 (EC2 is the only
capturer of that window). **NOTE (audit finding 6): the Mac still carries
`work/live/rest_disabled` — a plain reload resurrects the Mac WS-ONLY
(safe); to restore full Mac ownership (WS+REST) the flag must be REMOVED
first, and EC2 flagged/stopped to preserve the single-REST-owner rule.** **W-A5 must do the final Mac→S3 delta sync — the
Mac-ONLY residual is date=2026-07-09 hour 17:00→18:31** (vault has ≤hour 16;
EC2 self-captured from 18:30:57). Mac hours 19–23 are redundant (EC2 has
them) but sync them anyway (D1: more copies, never fewer).

## What W-A5 inherits

Daily EC2→S3 sync (+ prompt sub-daily during the fragile window) · final
Mac→S3 delta (hour 17→18:31 above) · detectors/alerts on EC2 · report
flow-back to the Mac Desktop · cost budget section · retire Mac
sleep-disable · metrics.ndjson rotation rider (a) still pending on EC2 (file
unbounded; 200 GB disk, not urgent) · vault_staging (85 GB) reclaim decision
· ticker-conflation fact → kalshi_facts.yaml candidate · **(audit finding 3)
supervisor tree ignores SIGTERM ⇒ every systemctl stop ends in SIGKILL
after 30 s — add a SIGTERM trap or KillMode tuning, with test** · events
crawl hits the 400-page/80k cap ("capped" surfaced, pre-existing — raise
cap or paginate by cursor date).
