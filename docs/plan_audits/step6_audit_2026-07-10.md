# STEP 6 combined independent audit (2026-07-10) — VERBATIM

Scope: the paper-only STEP 6 session's two drafts (PLAN_PRICING_MODEL.md +
PLAN_RISK_KILLSWITCH.md), audited together per GUARDRAILS P9 (docs-only
batching, one combined audit). Auditor: fresh-context agent, no part in
drafting.

Disposition (executor, same session): **B1 applied by tightening to the
operator's original instruction** — Group C is recv-clock exclusive for ALL
Ws; the pre-ladder shape-only idea survives only as a labeled OPTION that
requires an explicit operator ruling (E2) before any use. N1 fixed (external
anchor recorded as deferred + `external_anchor_lo` input slot reserved in
W-P2). N2 fixed (fee rounding regime read from kalshi_facts.yaml
ceil_to_centicent; sign test scoped to the pre-rounding curve). N3 fixed
(Group C strictly sequenced C1→C2→C3→C4). N4 fixed (dead-man expiry on
panic's own liquidation orders in W-K2 acceptance; ruling to be recorded per
E2 at execution). N5 fixed (W-K6 probe order defined as part of the
rehearsal). N6 fixed (queue claim aligned to MASTER_SEQUENCE verbatim). N7
recorded in SESSION_LOG (parent commit 6da7317 attribution + rtt sampler
owner ruling = operator item; also check_registry blind spot noted). N8
fixed (W-P4 seeded defects transient, never committed).

---

VERDICT: **ACCEPT-WITH-FINDINGS** — 1 blocking item requiring an operator ruling before Group C executes (which is gated anyway), 8 non-blocking. The operator's three red-line requirements are otherwise met; the verbatim fold and all named-item completeness checks pass mechanically.

## What I ran (per instructions A & H)

- **Verbatim diff (A):** extracted the `> `-prefixed lines from `PLAN_RISK_KILLSWITCH.md` §1 (`sed` §1→binding-map, `grep '^>'`, strip prefix) and diffed against `DESIGN_HOTPATH_EXECUTION_2026-07-10.md` lines 49–109 (§4–§5). Result: **byte-identical, zero diff** ("VERBATIM: IDENTICAL").
- **Completeness sweep (B):** grepped the binding map — all nine contracts #1–#9 have owners (`PLAN_RISK_KILLSWITCH.md:107-117`; #2/#3 explicitly tabled, not lost). Layers ①②③④⑤ all present in W-K3 with atomic-reserve + any-layer-short-rejects semantics (`:195-199`). All four named tests (a) Eggsy replay `:214`, (b) NHL template replay `:218`, (c) deadlock exemption `:221`, (d) refund path `:223` — each a named acceptance case with the ①④⑤-only red-first proof (`:230-232`).
- **Session discipline (H):** `git show --stat HEAD` = only the two docs (722 insertions); `git status` = working tree clean; no EC2/S3/config touched by the audited commit.
- **Fact greps (F), all TRUE:** `vol_1min_lo`/`tox_120s_lo` at `tools/mm_calibrate.py:104,107`; `FeeNotVerifiedError` at `tools/mm_research.py:192`; `config/kalshi_facts.yaml` exists (`fees.verified: false`, awaiting OQ-1 — consistent with the plan); per-worker `Lane` at `include/kalshi/client.hpp:149`; BADAMS 37¢→28¢/5min on zero trades matches `docs/MM_ROADMAP.md:42-44`; `FIRST_CLEAN_DAY = "2026-07-06"` at `tools/gate_calc.py:94` (7 days ⇒ earliest 2026-07-13 ✓ matches MASTER_SEQUENCE:82); `mm_backtest` recv default, fail-closed exit 3, `--allow-missing-recv` opt-in at `tools/mm_backtest.py:23-26,219-223`; `config/backtest_latency.yaml` placeholders confirmed; `warehouse.load` + lock-retry at `tools/warehouse.py:150,208`; alert path exists (`deploy/kalshi-alert.service` → `alert_notify.sh`); WP-01/WP-06 exist in `docs/EXECUTION_PLAN.md:87,182`; roadmap's old Phase-3 list = ①④⑤+per-order matches the design's claim. W-P1 golden math re-derived by hand: logit(0.02)−logit(0.01)=−0.703≈−0.70, logit(0.49)−logit(0.50)=−0.040≈−0.04 ✓.
- **Recv-clock honesty (C):** gate-date reasoning checks out against `docs/SESSION_LOG.md:27-93` — W-TL1 landed local-only 2026-07-10, EC2 adopts at "tomorrow's deliberate graceful-stop test" ⇒ ladder columns EC2-populated from 2026-07-10/11, so "go/no-go realistically ≥ ~2026-07-17" is honest, not a promise-slip.

## Blocking

**B1 — Group-C pre-ladder carve-out relaxes the operator's "clock=recv exclusively" requirement without an operator ruling, and self-contradicts on W-C1.** `PLAN_PRICING_MODEL.md:222-229` (gate box item 2) first states "Decision clock = recv for every fill-model / go-no-go run", then permits "Distribution-shape calibration (W-C1..C3)" on the pre-ladder (non-recv, exchange-stamped, ms-scale) archive. But W-C1 (`:232-241`) is precisely λ(δ) **fill-intensity** calibration with a pessimistic queue convention — fill-model parameters. λ calibrated on exchange-clock ordering can launder look-ahead into W-C4's recv-clock go/no-go via its parameters, defeating Q2's spirit while the go/no-go run itself stays formally clean. The operator's instruction said the calibration group "uses clock=recv exclusively"; the plan unilaterally narrows that to go/no-go runs only. It is disclosed in bold (honest, not hidden — hence not REJECT), but the §6 self-audit (`:350-353`) frames only the *date* mismatch as the flagged item, not this scope relaxation. Required: operator either ratifies the carve-out for C2/C3 shape work with W-C1 tightened to ladder-era recv data, or ratifies the carve-out as written — recorded per E2 before any Group-C W starts.

## Non-blocking

**N1 — Silent scope loss vs the roadmap being decomposed:** MM_ROADMAP Phase 1.5 A has a fourth fair-value component — external reference anchor (Crypto BTC/ETH spot index, which the roadmap calls "最大的单一优势"; Sports game_data deferred) at `docs/MM_ROADMAP.md:26-28`. `PLAN_PRICING_MODEL.md:31-33` rewrites A as three components and never records the external anchor as deferred. The sister plan has an "explicitly deferred (recorded so the audit sees intent, not loss)" §4; this plan needs the equivalent one-liner or the component is lost.

**N2 — Fee-rounding spec drift:** plan says "0.07·P·(1−P) rounded UP per contract" (`PLAN_PRICING_MODEL.md:62,113-114`), but the authoritative facts file the plan itself binds to says the current regime is `ceil_to_centicent` ($0.0001), superseding cent-level rounding (`config/kalshi_facts.yaml:34,42-46`). The acceptance clause "behavior follows the yaml, never hardcoded" saves it in practice, but the Purpose text contradicts its own source of truth, and the Q9 sign test "fee → 0 at extremes" (`:126`) is unsatisfiable on any post-rounding fee (min one tick) — specify it applies to the pre-rounding function.

**N3 — Overlapping Allowed writes:** W-C2 and W-C3 both own `tools/mm_calibrate.py` (`PLAN_PRICING_MODEL.md:255,271`) while §4 permits any-order execution — bounded (additive-only) but violates non-overlap between concurrent-eligible Ws; sequence them or split ownership.

**N4 — W-K2/S6:** the post-only tension is handled honestly (flagged for operator, `PLAN_RISK_KILLSWITCH.md:172-174,364-366`) and is in fact *stronger* than the plan claims — GUARDRAILS S3 itself prescribes "reprice-cross liquidation rounds" (`docs/GUARDRAILS.md:25-26`), so crossing is constitutionally sanctioned. Remaining gaps: the operator ruling must land in a file (E2), and S6's second clause (dead-man expiry on every resting order) is unaddressed for panic's own resting liquidation orders if a cross doesn't fill.

**N5 — W-K6 bootstrap circularity:** the rehearsal's probe order is itself the first live order, so S3's "rehearsed before the first live order" is satisfied only if the probe is read as part of the rehearsal. Operator-typed confirmation covers it in substance; one clarifying sentence would close the letter-of-the-law gap (`PLAN_RISK_KILLSWITCH.md:283-302`).

**N6 — Queue-position overclaim:** "Group M is executable immediately (this plan's approval is the only dependency)" (`PLAN_PRICING_MODEL.md:309-310`) overstates — MASTER_SEQUENCE STEP 6 verbatim says "Queue both for post-gate execution" (`docs/MASTER_SEQUENCE.md:79`); the plan's own next sentence correctly requires an operator sequencing ruling + amendment, which is therefore a real dependency.

**N7 — Session/branch hygiene (outside audited commit):** parent commit `6da7317` (75 min before the audited commit, same branch) committed non-paper content including `tools/rtt_baseline_sampler.sh` — the exact stray the W-TL1 session log flagged as "unregistered tool in tools/, E3 drift... needs an owner ruling" (`docs/SESSION_LOG.md:89-93`). It is absent from `tools.json` (grep exit 1) yet `check_registry` passes (125 tools ok), meaning the gate does not see shell-script drift. If `6da7317` was made by the same paper session, P9's docs-only batching was violated; needs attribution and an owner ruling either way. The audited commit itself is clean.

**N8 — W-P4 red-proof vs its own Forbidden writes:** seeded-defect runs require mutating `tools/pricing/*`, which W-P4 forbids writing (`PLAN_PRICING_MODEL.md:201-206`); clarify mutations are transient (monkeypatch/uncommitted), never committed.

## Explicitly verified clean (adversarial checks that found nothing)

Verbatim fold byte-identical; nine/five/four completeness perfect; recv-clock law present at all four required sites (header law, §1.7, gate box, W-C4 acceptance with the look-ahead-fixture demo); no live reach anywhere pre-gate (W-K1's one live call is read-class; W-K2 real mode is `live_order` console-forbidden and never executed); Q9 batteries are genuinely specified, not name-dropped (7 named tests W-P3, sign trio W-P2, red-first ①④⑤ proof W-K3); pessimistic bound + fees per Q2/Q3; P4 pipeline untouched by every W; both §6 self-audits' factual claims survived spot-falsification except the framing noted in B1.
