# W-P1 independent audit (2026-07-10) — VERBATIM

Auditor: fresh-context agent, no part in implementation. Scope: the W-P1
commit (log-odds core + operator-ruling-③ riders) against
PLAN_PRICING_MODEL §3 W-P1 and GUARDRAILS.

Disposition (executor, same session): N1 RESULT-block wording fixed (audit
outcome recorded, not pre-claimed). N2 reverse-scan scope + laundering
residual documented in check_registry docstring. N3 gate-before-enum locked
with two new tests (quadratic + maker_fee under gate_mode raise on
unverified yaml). N4 fee_raw↔trade_fee consistency test added. N5a tie-break
test added + N5b/c normative port notes in lo.py docstring and the PLAN
RESULT block. N5d (sys.path pattern) repo-wide convention, not changed. N6
recorded as a heads-up in the RESULT block for the OQ-1 ratifying session.
Suite: 13 → 15 tests, green; both gates re-run green after fixes.

---

## VERDICT: ACCEPT-WITH-FINDINGS (no blocking findings; commit 94f9d74 stays)

**What I ran:** `git show HEAD` (full diff); `./tests/run_pytest.sh tests/test_pricing_lo.py` → 13 passed; `python3 tools/check_registry.py` → ok (130 tools); full `./tests/run_pipeline.sh` → **PIPELINE PASS, 55 pass / 0 fail** (matches the commit's "55 suites" claim exactly); `make check` → ALL PASS; an adversarial Python probe battery (below); a temp-mutated `tools.json` (backed up, restored, `git diff` clean afterward) to attack the reverse scan.

**Independently verified (all check out):**
- **Goldens re-derived:** ln(49/99) = −0.7032995…, ln(49/51) = −0.0400053…, ratio 17.580 → "17.6×" in the RESULT block is honest ("~18x" in the test docstring also fine). Fee rows C=1: 0.07·0.25 = 0.0175 exact; 0.07·0.0099 = 0.000693 → ceil_to_centicent $0.0007 at both 1c and 99c. Maker 0.0175·0.25 = 0.004375 → $0.0044. All match `tools/pricing/lo.py` outputs.
- **Quantization torture:** exhaustive round-trip on all 99 cents, all 989 deci-cent prices (tick_e4=10), centicent samples incl. 9899 (tick_e4=1), each with lo perturbed ±1 ulp and side ∈ {bid, ask, None} — **zero failures; no on-grid price ever moves**. `lo_of_e4(9899)` on the cent grid correctly floors to 9800 / ceils to 9900 under ±1e-15…1e-7 perturbation. Band clamps land on [100, 9900] for tick 100/10/1 (the `0.01*10000` float-exactness worry is a non-issue; the ±EPS in the band computation covers it anyway).
- **Conservatism (check B):** fair 42.5c, δ_lo=0.01 → pre-grid targets bid 42.26c/ask 42.74c → quotes 42c/43c, edge **+0.5c both sides**; with directions swapped the same quote is 43c bid/42c ask, edge **−0.5c both sides** — rounding direction alone flips PnL sign. `test_quantization_is_conservative_off_grid` would catch a floor/ceil swap.
- **Fee gate (check C):** repo `config/kalshi_facts.yaml:32` still `verified: false`, so the last assert in `test_gate_mode_fail_closed_until_verified` (gate on the REAL yaml raises) is **not vacuous** — I reproduced it directly. Gate ordering in `maker_rate` is gate-BEFORE-enum, so `maker_rate("quadratic", gate_mode=True)` and `maker_fee(..., "quadratic", gate_mode=True)` both raise on the unverified repo yaml (probed) — the 0.0 short-circuit does **not** bypass the gate. This is the correct fail-closed semantics (the "quadratic makers pay nothing" claim rests on the same unratified provenance). Exactly one rounding-regime implementation exists (`mm_research.trade_fee:205-222`); probed `ceil(round(fee_raw·1e4,6))/1e4 == trade_fee` across 5 prices.
- **Scope (check D):** diff = `tools/pricing/{__init__,lo}.py`, `tests/test_pricing_lo.py`, `tools.json` (Allowed writes) + `tools/check_registry.py`, 4 registry entries, `tests/run_pipeline.sh` one suite line, PLAN RESULT block (all sanctioned riders per operator ruling ③ / BACKLOG 2026-07-10 / MASTER_SEQUENCE:96-103). Forbidden writes (pipeline, mm_backtest, config/*) untouched. The four registered scripts are tracked at HEAD (added earlier via 415ac7e/6da7317/ac2d2ad — the stale `??` in the session-start git snapshot predates the commit). Safety classes verified against script contents: rtt_baseline_sampler = credential-free bench_rtt REST burst → `network_read` ✓; rotate_metrics = local renames only → `offline` ✓; dashboard_net = operator-run read-only network console, `needs:["creds"]` → `network_read` ✓; load_db = stdlib json+sqlite3, no network imports → `offline` ✓. All four `autorun:false` ✓.
- **Reverse scan kill test (check E):** removing the rtt_baseline_sampler entry from a temp-mutated tools.json → `REGISTRY FAIL … E3 drift: tools/rtt_baseline_sampler.sh`. SCRIPT_LIBS entries all have no `__main__` (import-only, verified). Restored byte-identical.

### Findings (all non-blocking)

- **N1 — RESULT block pre-claims "audited"** (`docs/PLAN_PRICING_MODEL.md:135`): the committed heading says "DONE, audited" but this independent audit ran *after* the commit. D2-adjacent: a label that wasn't yet earned at commit time. Also the SESSION_LOG has no W-P1 exit entry yet ("golden table printed in the log" exit evidence is pending the exit ritual — the goldens do live in the RESULT block, so E2 is satisfied). Fix: the exit session should note this audit's outcome; future Ws should write "audit pending".
- **N2 — Reverse scan is defeatable by args_template laundering (demonstrated) and its scope boundary is implicit.** A bogus entry with `cmd: python3 tools/mm_scan.py` and `args_template: "tools/rtt_baseline_sampler.sh"` made the scan pass on a tools.json missing the real entry (`check_registry.py:99-103` counts any `.py`/`.sh` token as referenced). Acceptable residual for *accidental* drift (adversarial registry edits get audited anyway) but documented nowhere. Also: globs are non-recursive, so `tools/pricing/*.py` (import-only package — fine today), `tests/*.sh`, and future `work/mm/*.py` are silently out of scope; the docstring states the globs but not *why* subdirs/tests are exempt. One docstring paragraph would close both.
- **N3 — No test locks the gate-before-enum ordering in `maker_rate`** (`tools/pricing/lo.py:479-489`). The suite only gates via `quadratic_with_maker_fees`; a mutation moving `if fee_type == "quadratic": return 0.0` above the gate check would pass all 13 tests while returning a fee number under gate mode from unratified semantics. One-line test wanted: `maker_rate("quadratic", gate_mode=True, facts_path=unver)` raises.
- **N4 — `fee_raw` duplicates the pre-rounding polynomial** of `trade_fee` (documented why in its docstring — Q9 limit tests need pre-rounding — so the "delegation, single source of truth" claim is defensible for the *rounding regime*), but no test asserts `ceil_to_centicent(fee_raw) == trade_fee`, so silent divergence is possible. I verified consistency today by probe.
- **N5 — E7 port-spec gaps** for the Phase-2 C++ port: (a) `side=None` tie-break is round-half-up via `floor(x+0.5)` — untested and unstated as spec; (b) `_EPS=1e-9` semantics should be stated as normative (probe: a genuine off-grid value within 1e-9 tick of a grid line snaps to it — worst-case "tighten" ≈ $1e-11, immaterial, inherent to fuzz guards); (c) fees return **float dollars** (pre-existing WP-05 convention in `mm_research.trade_fee`) — prices respect the D5 integer-E4 boundary in/out, but the port must land fees on an integer centicent grid, worth one line in the plan; (d) `lo.py:380` mutates the importer's `sys.path` at import time (repo-wide pattern, but a port/packaging irritant).
- **N6 (observation, no action):** `test_gate_mode_fail_closed_until_verified`'s real-yaml assert will *fail* the day OQ-1 ratification flips `verified: true` — an intentional tripwire forcing a conscious test update; the ratifying session should expect it.

Acceptance criteria in PLAN §3 W-P1 are all demonstrably met; Q1/Q3/Q9/S2/D5/E1/E3/D2 hold; no forbidden write; no dishonest green. Repo left exactly as found (tools.json restored, verified clean; only `work/` test artifacts regenerated by the suite runs).
