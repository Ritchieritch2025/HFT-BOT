# INDEPENDENT AUDIT — PIPE-R001 W02 (day-seal rework)

- Date: 2026-07-11. Auditor: Cowork session (advisor/auditor role; did NOT implement W02).
- Scope: full diff `32ab4f6..cfc7c10` on branch `codex/pipeline-recovery-hardening`.
- Method: static/diff-level audit via the shared git object store. Runtime test
  execution was NOT possible in the auditor's sandbox (no duckdb, localhost
  restrictions) — see CONDITION below. Implementer reported machine gates green
  (make check 0 FAIL, run_pipeline PASS, 91 passed / 1 known xfail, registry ok).

## VERDICT: **PASS — conditional on pre-deploy gate re-run** (see CONDITION)

## Findings per required point (each verified in the diff/final tree)

1. **Zero seal deletion — PASS.** Only 4 `unlink/remove` sites exist in the
   touched tools; all legitimate: write-probe temp file (export_day.py:92);
   fail-closed deletion of the exporter's OWN just-written file on row-count
   verify failure, pre-seal (export_day.py:149, 488); seal-gated raw pruning
   (prune_raw.py:116). Seal invalidation exists only as
   `--operator-invalidate-seal <REASON>` which *parks* (never deletes) the seal
   plus ledger entry (export_day.py:558-561, 620+).
2. **Late data never mutates a sealed day — PASS.** ingest.py:281-283, 329-357:
   late rows diverted VERBATIM to `corrections/date=D/late_rows.ndjson`,
   counted in a corrections ledger, alerted; "The seal itself is never touched."
3. **Tamper fixtures — PASS (static).** test_pipeline_contract.py contains
   behavioral seal-tamper tests incl. archive mutation after exact verify,
   renamed archive column, uncheckpointed raw bytes, uncheckpointed
   cross-midnight receipt, stale seal after manifest change, unknown seal
   version/method, refused force preserving valid seal, late-fact diversion
   end-to-end. Red-state behavior re-confirmed at deploy per CONDITION.
4. **Seal chain off the capture path — PASS.** pipeline_supervisor.sh:
   single-instance flock; `run_seal_chain` runs in background with the explicit
   contract "capture never waits on seal work (P4)"; ws_shadow restarts first;
   watchdog documented as unable to affect capture continuity; durable
   `seal_alarm.json` with 02:00 earliest-attempt line and
   `UNSEALED_PAST_ALARM_LINE` alarm.
5. **prune_raw fail-closed — PASS.** Deletes only sealed days; retains
   cross-midnight (hour < threshold) files while previous day unsealed; retains
   unsealed dependencies; every retained file reported with a reason; dry-run
   supported; exceptions produce a report rather than deletion.
6. **legacy_v0 permanently non-gating — PASS.** Normal seals carry
   `go_no_go_eligible: true` (export_day.py:417); legacy seals hard-code
   `go_no_go_eligible: False` (export_day.py:706) with "forever" stated in the
   CLI help and unverified items listed (735 area).
7. **Research isolation — PASS.** warehouse.py enforces an archive-only source
   boundary (past windows never attach live staging; crossing the boundary
   closes/invalidates cached live connections, 311-319); gate/mm tools surface
   unsealed/legacy grades instead of silently skipping (per test
   `test_default_past_window_is_sealed_archive_only` and grade plumbing).
8. **Scope containment — PASS.** `git diff --name-only 32ab4f6..cfc7c10`
   touches no GUARDRAILS, no `src/ apps/ include/` engine code, no `main`;
   production runtime unchanged until the operator-approved deploy.

## CONDITION (blocking for deploy, not for merge readiness)

The deploying session MUST re-run, on the machine that deploys, immediately
before the maintenance window: `make check`, `tests/run_pipeline.sh`, and the
seal test files (test_pipeline_contract.py, test_warehouse_nonuniform_archive.py,
test_export_day.py) — all green, outputs quoted in the deploy report. This
covers the runtime verification this audit could not execute.

## Backlog notes (non-blocking, do NOT trigger another revision round)

- B1: `verify_raw_caught_up` TOCTOU window is handled at seal time only —
  acceptable per ruling ("verify once at seal, trust the seal after").
- B2: corrections partition currently ndjson-only; a reader/compaction tool can
  be a later W.
- B3: seal_alarm.json consumer (Telegram/dashboard) pending the alerts W.
