# E4 对抗复核检查单(预注册)

**注册时间:** 2026-07-25,毫秒普查(③)重跑中、E4 未开建、任何结果未产出。
**复核人:** 本线(与建模线分离)。本单在看到任何 E4 数字之前写死;
复核时逐条打钩,任何一条不过 = E4 结论不得用于 $200 探针开闸。

## A. known-answer(②)
- [ ] A1. 有收据证明坟场格(最后 2 分钟 mid,KXBTC15M)在正式格子之前先跑,
      输出为深红(数量级对得上 E2 的 −1.70¢/张,符号错或浅于 −0.5¢ 都算失败)。
- [ ] A2. known-answer 失败后的修改(若有)有变更记录,且修改后没有"顺手"
      改动会影响正式格子的参数。
- [ ] A3. 反向 known-answer:模拟器在"永不成交"的极端参数下输出 ≈0 笔成交
      (防止引擎无条件产生成交)。

## B. 成交模型(①,对标审计十条)
- [ ] B1. 队尾假设:我方挂单排在同价位现存数量之后,无插队。
- [ ] B2. 只认穿价成交(悲观口径),touch 成交只出现在乐观对照列。
- [ ] B3. 撤单延迟不是单一假设值:100/300/800ms 三档扫出盈亏-延迟曲线;
      结论引用的那一档有明确标注。
- [ ] B4. 我方成交的对手方向分布 vs 全场分布有对比表(毒性不对称检查——
      悲观模型下我方吃到的毒单比例应 ≥ 全场平均,若更低需解释)。
- [ ] B5. 成交率(我方成交/触碰机会)有报告,且与 touch 队列长度(③普查)
      数量级自洽。
- [ ] B6. 结算记账用真实结算结果(catalog),非模型推断。

## C. 统计纪律(④)
- [ ] C1. cluster = 单个事件窗(15 分钟/1 小时),所有显著性按 cluster 计算,
      不按逐笔。
- [ ] C2. 格子挑选只发生在 TRAIN(07-12..19);VALIDATE(07-20..23)只出现
      "选定格子的复验",无新格子入选。
- [ ] C3. VALIDATE 上格子保持同号且幅度 ≥ TRAIN 的 1/3(防 TRAIN 过拟合)。
- [ ] C4. 灰格规则无例外:n(cluster)<门槛的格子在结论表中标灰,
      正文没有引用任何灰格数字。
- [ ] C5. 结论没有跨 series 混池表述(分层裁决,逐条 series 判)。

## D. 输入承重件
- [ ] D1. ③普查产出了 markout 衰减 0→1000ms 段(L2 2ms 分辨率),
      "1s 后走平"被重验;若 1s 内仍在恶化,E4 输入的渐近 markout 已相应下调。
- [ ] D2. 狙击 p10 vs 我方回路(15–60ms 假设)对表存在,且 E4 撤退纪律
      参数与之一致。
- [ ] D3. null 数量防御(07-25 修复)有单元测试或抽查收据,重建订单簿与
      任一日 L1 快照抽样交叉核对(≥100 个时点,touch 价一致率 ≥99%)。

## E. 开闸判据(预先说死,防移动球门)
- [ ] E1. 开闸线 = E4 悲观口径(队尾+穿价+实测或 800ms 撤单延迟)在
      TRAIN 与 VALIDATE 双段均为正,且为 cluster 级 t≥2。
- [ ] E2. order_latency_test(demo 实测下单/撤单往返)在探针启动前完成,
      实测值落在盈亏-延迟曲线的正值区间内。缺这一项,探针不开,
      无论模拟多绿。
- [ ] E3. 探针方案含硬性 kill 条件(单日亏损上限、成交率异常、
      markout 实测偏离模拟 >2×)。

**任何"检查单外的新理由"要求放行时:记录、拒绝、上报 principal 裁决。**

## 增补条款(2026-07-25,③/E4 出数前,principal 指示)
- [ ] F1. ③ 的五个钟按(剩余时间 × 波动桶)条件化输出;正文引用的任何
      寿命/狙击数字必须带桶标签,无条件全局分位数不得作为结论依据。
- [ ] F2. E4 报价规则含波动门(现货/代理波动超阈值撤单),阈值经过扫描,
      且盈亏按波动桶分解报告(防止全部利润来自本该离场的暴走态)。
- [ ] F3. "可玩格"三条件齐备才算绿:寿命 p10 ≥ 5× 回路、桶内 taker 流量>0、
      桶内毒性可容忍;缺一标灰。
- [ ] F3a.(2026-07-25 修订,③v2 出数后、E4 未开建)F3 的寿命判据按策略形状
      修正:战术已从"反应式做市"定型为"提前站位+状态撤退",我方挂单本来就
      预期在风暴中被穿价成交,不要求盘口寿命 ≥ 回路。改为判**决策时间尺度**:
      (i) 入场站位发生在 calm/mid 态(秒级,≥5× 回路,可执行);
      (ii) 撤退触发器(burst 探测、T−5m)的探测→全撤完成延迟有实测或保守上界,
      且 E4 盈亏在"风暴内撤不掉"(撤单在 burst 内视为无效)的最坏假设下仍为正;
      (iii) tick 级躲闪不得作为任何格子盈利的必要条件(普查已判死:狙击 p50
      11–13ms < 回路下界)。
- [ ] F4. 状态标签的循环性检查:burst 标签必须用**严格先行**的滚动窗计算
      (窗口不含当笔成交本身);报告必须给出 burst 态的**时间占比**作对照
      (95% 成交发生在 burst——若 burst 只占 5% 时间,结论锋利;若占 50%
      时间,结论平庸;缺这个分母,"95%"无法解读)。
- [ ] F5. E4 队列建模必须跨状态转移追踪我方队位:calm 态入队(队深 ~1,455)
      → burst 态队列消耗到 ~181 的过程中,消耗来自成交(推进我方队位)还是
      撤单(前方撤=推进,后方撤=不动)必须用 L2 逐笔区分;悲观口径 = 前方
      撤单不计入推进。"提前站位"论点的全部价值就在这一条的账里。
