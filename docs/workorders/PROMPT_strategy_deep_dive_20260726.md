# PROMPT — 策略完整展开(给策略 agent,原样粘贴)

你是本仓库的策略负责人。任务:把 `tools/research/crypto_mm/mm_engine.py` **当前实际运行**的策略完整展开,写成一份《策略白皮书》,交给决策人逐条审判和调参。只描述代码里真实存在的行为,每条结论必须附代码定位(函数名或行号)。禁止写"应该是"的行为——只写"现在是"。

## 输出结构(严格遵守)

### 1. 一页决策树
从一个 tick 进入引擎,到最终"发单 / 改单 / 撤单 / 不动 / HALT",用缩进文字画出完整决策树。每个分支节点标注三件事:判断条件、阈值当前值、阈值来源(硬编码 / 环境变量名 / 控制文件字段)。此节必须一页内读完。

### 2. 公式清单
表格,每行一个公式:数学表达式 | 代码函数 | 输入及单位 | 输出及单位 | 当前参数值。必须覆盖:
- σ 估计(60s/300s、max 取法)
- 结算均值方差两阶段公式(>60s 与最后一分钟 locked_sum)
- Φ 映射到 fair
- edge 计算与 2.5¢ 门槛
- 库存 skew(当前 0.5¢/张 线性)
- taker 费用公式
- 配对门槛(98.5¢)与孤腿削减规则
单位错误零容忍:¢、$、概率、σ、秒必须明确区分。

### 3. 可调旋钮总表
表格:参数名 | 当前值 | 修改方式(env/控制文件/改代码) | 调大后果 | 调小后果 | 建议安全范围 | 与哪些参数联动。这是决策人调参的操作面板,漏一个参数算失败。

### 4. 数据依赖与失效行为
每个输入源(RTI、Kalshi 盘口、成交流、账户/结算):新鲜度要求、失效判定、失效时引擎行为(暂停/HALT/降级)、代码定位。

### 5. 判决书六条的代码现状 + trend_signals 接线方案
对照 2026-07-26 判决书六条,逐条写明当前代码状态:已修 / 工单中 / 未动,并给证据。
然后针对 `tools/research/crypto_mm/trend_signals.py` 的六组函数,逐个给出接线方案:

| 函数 | 作用 | 建议接线点(engine 具体函数/步骤) | 默认参数 | 需要先校准什么 |
|---|---|---|---|---|
| `momentum_z` | 多周期动量 z 分 | | | |
| `jump_flag` / `rv_bv_ratio` / `JumpCooldown` | 跳跃检测与冷却撤单 | | | |
| `drift_adjusted_mu` | 均值加动量延续项(β) | | | β 回归(66k 干净增量) |
| `required_edge_c` | 逆势侧非对称加价 / 拒报 | | | γ、hard_z |
| `flow_imbalance` / `flow_confirms` | 盘口 taker 流确认动量 | | | |
| `inventory_skew_c` / `dynamic_clip` | 波动率标度的 skew 与动态仓量 | | | η、z0 |

每个接线点必须说明:插在决策树第几个节点之前/之后、shadow 模式如何先行验证、以及**离线验收标准**——用已有 1600 笔影子成交回放:该门若当时开启,能拦掉多少负 markout 成交、误伤多少正 markout 成交(给出两个百分比)。

### 6. 需要决策人拍板的问题
列出所有你无权单方面决定的取舍(至少包括:是否启用 β 漂移项还是只用非对称 edge 门、hard_z 取值、2.5¢ 门槛是否应两侧不对称、jump 冷却时长、配对 admit_gap 臂是否保留)。每个问题给:选项、各选项预期影响、你的建议与理由。

## 纪律
- 每条结论标注来源等级:[代码事实] / [实测数据] / [假设未验证]。
- 不确定就写不确定,不许编。
- 数字给出处(哪个日志、哪次实验、哪行代码)。
- 长度不限,清晰优先;第 1 节必须一页内。
