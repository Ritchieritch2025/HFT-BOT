# W09 研究机就绪状态 — 2026-07-15

**一行裁决:✅ 软件栈装齐并实测通过,可供新 session 开 autoresearch;
⚠️ 一个非软件前置未走:正式 W09_READY 需一次真实空闲关机证明(操作员观察)。**

## 实测就绪项(全部本 session 亲验)

| 项 | 状态 | 证据 |
|---|---|---|
| 实例 | 开机,us-east-2,r8g.2xlarge,8 核 / 61 GB | SSH up；`i-0e53d134dceffe166` |
| 角色 | `w09-research-runner`,IMDSv2 临时凭据 | metadata 实测 |
| S3 权限 | 只读 `research/*`(List 整桶 + Get/GetObjectVersion),**无写、无越界读** | instance-profile inventory 列出全 6 release |
| Python | 3.12.3,venv `/opt/w09/venv` | — |
| DuckDB | **1.4.5**(与生产同版本,断言通过) | `duckdb=1.4.5 OK` |
| 研究栈 | numpy 2.5.1 / pandas 3.0.3 / scipy 1.18.0 / statsmodels 0.14.6 / matplotlib 3.11.0 / pyarrow 25.0.0 | ALL-IMPORTS-OK |
| canonical CLI | `/opt/w09/research/tools/`(research_data + instance_profile + select_newest + warehouse_common),SHA 门在 push_and_install | — |
| 空闲守卫 | 武装中(active),1800s 无 SSH/inhibitor/后台任务才关机;`w09-run` = 设计抑制器;`/etc/w09-idle.disabled` = 一键停用 | 装 venv 时挂 w09-run inhibitor,装完自动释放、已复武装 |

工具选型(已在库中定型,DuckDB 优先):重活(多日 join、L2 replay、
bootstrap、上亿行扫描)走 **DuckDB SQL**;策略数学 **numpy 在 log-odds 空间**;
统计校正 scipy/statsmodels 或 numpy 手写(BH/Holm/bootstrap);图表 matplotlib
或内联 SVG;小结果集才用 pandas。**不要把 68GB 塞进 pandas —— 会 OOM。**

## 唯一剩余前置(非软件)

`run_acceptance.sh` fail-closed 要求**上一轮**有一次
`idle_for_sec >= 1800` 且不同 boot id 的空闲自动关机事件 —— 现在这台是
刚开的新 boot,`/var/lib/w09-research/` 无该证明。这是 README 第 2 步
("实测 30 分钟真关机")还没走。走法(操作员 + agent 配合):
1. 关掉所有到 W09 的 SSH,别缩短超时;
2. 约 30 分钟后实例自动 `running→stopping→stopped`(操作员在控制台观察);
3. 操作员重启同一实例;
4. 重启后 30 分钟内,agent 跑
   `W09_CONTROL_PLANE_STOP_OBSERVED=stopped bash deploy/w09/run_acceptance.sh`
   → 内部做 inventory + 含 sealed RFQ 的 exact-VersionId fetch + verify
   → 打印 `W09_READY`(同时完成 SUPERSEDE 第 2 条的 RFQ 验证)。

**注意:软件就绪 ≠ W09_READY。** 新 session 可以立即用 W09 做探索计算
(inventory/fetch/DuckDB 都通),但图纸 GATE B 的正式 W09_READY 凭据要等
上面这次关机证明。若新 session 一开就要正式凭据,先走这步。

## 新 session 起步锚点

- 图纸:`docs/plan_audits/SPORTS_AUTORESEARCH_01_MISSION_TEXT_V2_2026-07-15.md`
  (sha256 c4c31674571c8c37fd7b761b125064cecf154e9150cba49356d6df50196342c6,
  **待操作员钉定发布**)。
- 数据:6 release / 80 GB,核心 = 07-12 + 07-13 带 sealed RFQ 两日
  (`…pub-2bf8871ad4750c03` / `…pub-f8e4c0abc742b7d5`)。
- 层级:全部 SEALED_DEGRADED_EVIDENCE = 探索级(横幅强制,禁 verdict)。
- 报告形态:`docs/AUTORESEARCH_REPORT_REQUIREMENTS.md`(单页审阅、逐一过目)。
- W09 连接:`ssh -i ~/.ssh/kalshi-key.pem ubuntu@18.226.151.192`;venv
  `/opt/w09/venv/bin/python`;缓存 `/srv/w09-research/cache`;
  重活挂 `sudo /usr/local/bin/w09-run <cmd>` 防空闲关机。
