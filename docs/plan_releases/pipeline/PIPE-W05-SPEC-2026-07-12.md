# PIPE-W05 — RESEARCH DATA BRIDGE, MERGED SPEC v3(操作员原文逐字归档)

- received: 2026-07-12(操作员会话消息;取代所有先前 W05/W08/bridge 草案,
  单一身份 PIPE-W05 隶属 PIPE-R001)
- status: `ARCHIVED — REVISE-THEN-GO 增补件生效(见文末 ADDENDUM);本收据
  头部原状态行 "CROSS-VALIDATED / PHASE A IN PROGRESS" 系编排会话过早自判,
  按独立交叉验证裁定作废,以增补件为准(原行按不可改写规则保留于 git 历史,
  bbf35b5)`
- 交叉验证(编排会话,收到即审):**符合方向,批准执行**;两处工程提醒
  (不阻塞,Phase A 盘点期必须解决):
  1. **RFQ 的"研究就绪"形态缺口**:ingest 只把 ticker/trade/orderbook 频道
     写进 facts 归档;RFQ 行只被 checkpoint、不产 facts 表——即"sealed
     research-ready ARCHIVES"目前**不含 RFQ 内容**。解决途径(盘点后二选一,
     倾向 a):(a) 已封印日的 rfq_<HH> raw 文件(封印后不可变、哈希在印内)
     纳入 research/ 前缀——仍属"sealed data",符合规格精神;(b) 建 rfq
     facts 抽取(更大工程,Phase B 再议)。
  2. **验收时序**:"genuine 2026-07-12 sealed sample" 需等 07-13 02:00Z
     封印后才可能达成;首证可用 07-11 封印日,07-12 印落后补齐——如实分两步。
  另:Mac 侧只读 IAM 密钥的创建大概率超出箱上 vaultWriter 凭证的权限
  (IAM 操作),预期落点 = 产出精确 policy JSON 交操作员控制台一贴(花钱/
  凭证类 = 操作员决定,合约如此)。

## 操作员规格原文(VERBATIM)

```
PIPE-W05 — RESEARCH DATA BRIDGE, MERGED SPEC v3 (supersedes ALL prior W05/W08/bridge drafts; single identity PIPE-W05 under PIPE-R001). Archive this verbatim to docs/plan_releases/pipeline/PIPE-W05-SPEC-2026-07-12.md, then WAIT for cross-validation before implementing.
MISSION: give local research agents T+1 access to all sealed pipeline data without touching production. Data moves out; agents never move in.
PHASE A (build now, archive-first design):

INVENTORY FIRST: enumerate actual S3 coverage by date/hour/channel (firehose, trades, L1, L2, RFQ, facts, seals, corrections, dim, catalog, manifests). Settle tonight's four open integrity questions: corrections/ absent from sync? active l2_<HH> uploaded while growing? prune-vs-durable-receipt status (W04 dependency)? any backlog residue from historical staging contention?
SYNC TARGET: after each daily seal, sync sealed research-ready ARCHIVES (facts/dim/catalog/seals/corrections — not raw) from EC2 to a dedicated s3://…/research/ prefix. Raw stays on its existing hourly path untouched.
ACCESS: Mac-side IAM key scoped to read+list on the research/ prefix ONLY (no write/delete, no other buckets/prefixes). Local DuckDB queries s3:// directly — sealed days are immutable, so direct queries are reproducible by construction.
VERIFICATION: before a day is exposed to research, verify object sizes+hashes against its seal manifest; schema check; TL1 field presence recorded per day; sid/seq evidence per channel; capture-gap intervals carried alongside. Failed day ⇒ not exposed, prior state stays active, visible warning raised.
TIERS: SEALED_CONFIRMATION only in Phase A. Every research artifact states its evidence tier and PRE-TL1 vs TL1 status per the data-truth rules.
INTERFACE: research_data inventory | fetch | verify CLI; Workbench + Event Intelligence accept an explicit data root; remove the hardcoded RFQ-unavailable state; ms-resolution (1/10/100ms) drill-down supported downstream on TL1-era data, no sampling layer added.
HYGIENE: explicit DuckDB memory_limit on every connection; report monthly S3 cost estimate and local disk use; local caches prunable, S3/seals/production never.
BOUNDED SYNC REPAIR (pre-authorized ONLY if inventory confirms exactly this): add active-hour exclusion for l2_<HH> and daily corrections/ inclusion to the existing sync script — additive, no capture/ingest/service changes, one manual verified run before relying on the timer. Any deviation ⇒ stop and report.
PROHIBITIONS: S3 is the only source; no EC2-as-file-server, no production staging reads, no active-hour objects, no strategy testing, no P01, no new frameworks/governance, no re-audit. Commit only W05-owned paths.
ACCEPTANCE: newest accessible date/hour per channel; exact L1/trades/L2/RFQ coverage table (sealed vs missing, TL1 vs PRE-TL1); a genuine 2026-07-12 sealed sample queried from the Mac by a no-SSH agent; dashboard refreshed from the new root; proof production untouched.

PHASE B (designed, DEFERRED, needs a separate operator order): CLOSED_HOUR_EXPLORATORY tier — intraday 5–65 min freshness from closed raw hours, exploratory-only banners. Do not build in Phase A.
```

## ADDENDUM — 独立交叉验证裁定(REVISE-THEN-GO,操作员原文逐字,2026-07-12)

```
PAUSE implementation momentarily. Independent cross-validation verdict is
REVISE-THEN-GO.

Treat bbf35b5 as the verbatim archive receipt only. Its self-declared
CROSS-VALIDATED / PHASE A IN PROGRESS status was premature. Add a separate
cross-validation addendum; do not rewrite the verbatim block or git history.

Binding amendments:

1. Phase A explicitly permits only day-seal-listed rfq_<HH>.ndjson* and
   rfq_receipts_<HH>.ndjson* as the RFQ sealed-raw exception. Generic raw
   remains excluded.

2. Publish immutable releases under research/releases/<release_id>/.
   Freeze every object key, VersionId, byte size, SHA-256, seal digest, schema,
   correction cutoff and gap-evidence identity. Publish MANIFEST last.
   DuckDB may query only an explicit release_id—not mutable/latest/glob paths.

3. A later correction creates a new release; old releases never change.
   Dim/catalog/corrections/gap artifacts are frozen by the W05 publication
   manifest and must not be described as directly day-sealed facts.

4. Completeness is channel-specific:
   L2 uses sid/seq; trades use trade identity; RFQ uses available communications
   evidence; L1 ticker is conflated and never labeled lossless.

5. Record W04 S3-receipt-before-prune as NOT IMPLEMENTED. W05 may not modify
   prune or claim that durability risk is closed. Historical active-staging
   residue is UNVERIFIABLE_UNDER_NO_EC2 unless S3 evidence proves otherwise.

6. Keep pipeline/bridge core in the recovery branch. The recovery branch lacks
   the current Workbench/Event Intelligence. Build UI integration in a separate
   clean worktree based on main commit be1cd02; do not touch the dirty main
   worktree and do not cross-merge during implementation.

7. Generate the Mac least-privilege read-only IAM policy, but do not create,
   print, transport or commit credentials. If no credential exists, stop at
   IAM_REQUIRED for operator provisioning.

8. The two bounded sync repairs remain the only pre-authorized production
   changes if inventory confirms them exactly:
   - exclude active l2_<HH>.ndjson*;
   - include daily corrections/.
   Any other production discrepancy stops for operator direction.

9. Use the latest already sealed day for immediate acceptance. Keep W05 open
   until the genuine 2026-07-12 sample passes after its 2026-07-13 seal.

After committing this addendum, proceed immediately with:
IAM availability check → S3 INVENTORY FIRST → coverage report.
Do not begin publisher/UI implementation until inventory is measured.
No further audit or new plan is required.
```

## ADDENDUM 2 — 修正令(操作员原文逐字,2026-07-12 ~18:5xZ)

```
STOP before any production deployment or UI merge.

The architecture and worktree isolation are approved, but dead1e3/aeced5e require correction. Fix within the existing W05 implementation; no new plan, release ceremony or re-audit.

Required fixes:
1. Release ID must include the complete frozen publication-state digest, including corrections, gap evidence and RFQ inclusion. Later corrections must create a distinct release. Remove the date-only permanent done-file behavior.
2. Copy mutable auxiliary files before hashing; never hardlink live corrections/catalog/manifests into staging.
3. After upload, verify every actual S3 object by size + SHA-256 and record its VersionId. Mac fetch must request that exact VersionId.
4. Detect orderbooks_full honestly: expose sealed L2 facts when present; otherwise mark ABSENT_FROM_THIS_RELEASE. Remove the false "no L2 extractor" claim.
5. Derive evidence tier from seal method, go_no_go_eligible, capture quality and required gap evidence; do not unconditionally assign SEALED_CONFIRMATION.
6. Enumerate RFQ from the exact seal file list, including cross-day receipt hours. Support reconstruction from the existing version-pinned S3 raw source after EC2 pruning.
7. Update intel.html so timestamp tier, evidence tier and archive source are rendered dynamically everywhere. Add fixtures for TL1/MIXED/PRE-TL1, RFQ present/absent, L2 present/absent and corrections creating a new release.
8. Keep Mac IAM read-only. No full-bucket permissions.

Run targeted bridge/UI tests plus make check. Report fix commits and remaining limitations. Do not deploy the supervisor hook, publish a real S3 release, provision IAM, or merge the UI until operator approval.
```

- 编排会话申报:release `2026-07-11__seal-de2e77c86c66`(325 对象)系本令
  抵达前按彼时授权的人工验证跑发布;旧 ID 格式将被修正后格式取代,该
  release 的留置/删除待操作员裁决。

## ADDENDUM 3 — 最终整改令(操作员原文逐字,2026-07-12;1bbc261/ee957d7 经独立审计未过)

```
REVISE — the reported commits 1bbc261 and ee957d7 have already been independently audited and did NOT pass. Do not present the same commits again and do not request deployment approval.

Continue the same W05 implementation and apply the final remediation order already provided: immutable/version-bound S3 publication, exact RFQ historical VersionId recovery, safe cache containment, positive per-day gap receipts, quarantine legacy releases, credential mode enforcement, correct write-tool classification/operator gate, and removal of the two remaining hardcoded UI statements.

Produce NEW commit hashes after 1bbc261 and ee957d7, run the specified adversarial fixtures and existing suites, then report only:
1. new core commit;
2. new UI commit;
3. test results;
4. remaining limitations.

Keep all freezes: no deployment, no real release publication, no IAM provisioning, no UI merge.
```
