# 下一阶段数据队列 — 优先级计划(送审版,已被 v2 取代)

**Status: SUPERSEDED — 独立审计 2026-07-16 提出六条 FINDING,全部采纳。
权威版本 = `PLAN_NEXT_PHASE_DATA_QUEUE_EN_2026-07-16.md`(v2,双轨结构)。
本文件仅留档。主要修订:结算改为可回填(非时间债);恢复"先还数据债
再加采集负载"顺序;零复制升 P0 并补全三处改动(发布器/读取器/IAM);
新增主数据逐对象收据前置;S3 转冷降为最后且带引用保护;接收钟指名
local_recv_ts_us。**
**起草:**操作员陪跑 session(Claude),2026-07-16,基于当日数据层全景
勘查(`docs/DATA_LAYER_OVERVIEW_2026-07-16.md`)与本周全部已核事实。
**排序唯一原则:按利息排。**时间债(今天不做 = 该日数据永久缺失)最先;
一次性解锁项次之;架构/成本优化最后。

**审计要点(请审计者重点核对):**①每项的"依据"是否真实存在于所引文档;
②生产变更项的安全约束是否充分;③排序逻辑是否有更优反例;④与
GUARDRAILS、MM_ROADMAP、既定禁令有无冲突。

---

## P0-A · L2 深度采集扩容(时间债,本周)

- **现状与依据:**定向 L2 每小时仅约 50 个市场,实测有效数曾低至 4
  (DATA_LAYER_OVERVIEW);deep02 头号候选(深度蒸发/拉锯区做市)依赖
  深度信号;B14 坏时间戳未修。
- **内容:**①扩容目标选择逻辑:优先覆盖体育活跃比赛市场(赛中+临近
  开赛),给出扩容后带宽/存储/成本估算,**先报数字过操作员再部署**;
  ②同 W 修复 B14 时间戳缺陷;③扩容部署走 capture-continuity 审查
  (生产采集变更,主 firehose 不许受影响,L2 失效只降级不传染——
  维持现有 fail-closed 设计)。
- **验收:**扩容后有效 L2 市场数实测报告;B14 修复带回归测试;
  主 firehose 前后心跳零异常。
- **明确边界:**不做全市场 L2(数据量不可承受,维持定向抽样架构)。

## P0-B · 生命周期与结算数据采集(时间债,本周)

- **现状与依据:**回测无法计算持仓到结算的真实盈亏;97-99 候选完全
  不可评估;仓库无权威开赛时间(Kalshi 无此字段,warehouse_schema)。
- **内容:**①新增采集:市场状态变迁(开/停/关/结算)与结算结果,
  独立原始数据族(与 firehose 同信封纪律,byte-exact);②开赛时间
  用市场元数据快照 + 行为反推做代理,**不采购外部赛程 API**(维持
  禁令;仅当"失明成本>API 成本"被量化证明后另行请示);③新数据族
  纳入封存/发布/体检全链。
- **验收:**采集运行 48h 后抽一日核对:状态变迁完整性、结算值与
  交易所页面一致(抽样 n≥20)。

## P0-C · 封存健康(20 天时钟地基,本周)

- **现状与依据:**已建成的封存修复(W-A 三合一,SESSION_LOG 07-14
  23:05 条目 BUILT 未部署)等独立审计;两个 session 对"哪些天已封存"
  口径矛盾(修复 session:07-09~15 连续 vs 数据勘查:封存反复故障)。
- **内容:**①对账:输出权威的"逐日封存状态表"(哪天有 seal、
  verify 是否 PASS、发布状态),钉死唯一事实;②W-A 独立审计
  (审计令已在 SESSION_LOG)→ PASS 后部署;③部署走 capture-continuity。
- **验收:**逐日状态表落档;W-A 部署后连续 3 日封存无人工干预。

## P1-A · 费率核实(半天,随时插空)

- **现状:**fees.verified=false;一切"扣费后净收益"数字被闸。
- **内容:**对照 Kalshi 官方费表 + 实测(历史成交回算/只读接口),
  逐产品线钉死 maker/taker 费率,写入 facts(带出处三态标记)。
- **验收:**fees.verified=true;RISK_INDICATOR_FRAMEWORK A3 与
  BACKTEST 费用模型引用同一 facts。

## P1-B · Receive-clock 纪律落地(半天)

- **现状:**接收时刻(recv 纳秒)在数据里一直有,但研究工具默认键在
  交易所时间列 ts_utc = 回放偷看未来(DATA_LAYER_OVERVIEW)。
- **内容:**①盘点键错列的工具清单;②能改的改默认列;③改不动的
  在输出头部打 DIAGNOSTIC_ONLY_EXCHANGE_CLOCK 硬标;④回测契约重申:
  裁决级数字唯一合法时间轴 = receive clock。
- **验收:**工具清单 + 逐项处置记录;抽一工具改前改后对比留档。

## P2 · 资源与架构(下周)

1. **W-PUB-REF-01 零复制发布**(已批,含禁删保障 + 引用巡检两条硬性)。
2. **S3 生命周期降级**:老对象自动转低频/归档层(一条配置;
   预估月费砍半,数字报操作员)。
3. **W-B9-BATCH-01 批量通道**(已有计划文档;deep03 开庭前到位)。

## 维持禁令(重申,不因本计划松动)

研究自动控制器(记台账不排期)、更多 Telegram 功能、更多纯描述性报告、
RFQ 任何修复、复杂 ML、外部数据 API 采购、实盘部署、AUTO_RESEARCH=1、
自动化旧复制式 publisher。

## 通用纪律

全部 W:五条刹车、一 session 一 W、完工独立审计、PLANS_LEDGER 登记、
退出仪式。生产变更(P0-A/B/C)额外:capture-continuity 审查 +
部署前后采集心跳留证。费用:单项新增经常性成本 >$100/月逐笔请示,
其余月报(既定分级政策)。

## 本计划的自检(供审计者反驳)

- 为什么 L2 扩容先于费率?——费率半天且随时可做,不产生时间债;
  L2 每晚一天 = 头号候选永久少一天证据。若审计认为费率更急,
  两者本就可并行,不构成冲突。
- 为什么不先做 B9?——B9 加速的是"重算已有数据",不影响数据存量;
  deep03 前到位即可。
- 为什么生命周期不买外部 API?——禁令 + 失明成本未量化;
  代理方案先行,量化后再议。
